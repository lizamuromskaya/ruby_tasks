# frozen_string_literal: true

# Define a method sum(array) that takes
# an array of integers as an argument and returns the sum of its elements.
# For an empty array it should return zero.
# Run associated tests via: $ rspec -e '#sum ' spec/part1_spec.rb
def sum(array)
  array.sum
end
